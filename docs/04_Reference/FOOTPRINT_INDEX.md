# Footprintbibliotheken

> Diese Datei wird mit `python tools/generate_library_reference.py` erzeugt.

Die Footprintbibliotheken liegen als `.pretty`-Ordner unter `footprints/`.

**Anzahl der Bibliotheken:** 35

## Bibliotheken

- `Z_Busbar_1P.pretty` — vorbereitet, noch leer
- `Z_Busbar_2P.pretty` — vorbereitet, noch leer
- `Z_Busbar_4P.pretty` — vorbereitet, noch leer
- `Z_Busbar_Comb_3P.pretty` — vorbereitet, noch leer
- `Z_CONTACTOR.pretty` — vorbereitet, noch leer
- `Z_DIN_Contactor_45mm.pretty` — 1 Footprint(s)
  - `Z_DIN_Contactor_45mm.kicad_mod`
- `Z_DIN_Control.pretty` — vorbereitet, noch leer
- `Z_DIN_Electrical.pretty` — vorbereitet, noch leer
- `Z_DIN_Module_18mm.pretty` — 1 Footprint(s)
  - `Z_DIN_Module_18mm.kicad_mod`
- `Z_DIN_Module_36mm.pretty` — 1 Footprint(s)
  - `Z_DIN_Module_36mm.kicad_mod`
- `Z_DIN_Module_45mm.pretty` — 1 Footprint(s)
  - `Z_DIN_Module_45mm.kicad_mod`
- `Z_DIN_Power.pretty` — vorbereitet, noch leer
- `Z_DIN_Safety.pretty` — vorbereitet, noch leer
- `Z_DIN_Safety_Module.pretty` — 1 Footprint(s)
  - `Z_DIN_Safety_Module.kicad_mod`
- `Z_DIN_Terminal_Block.pretty` — 1 Footprint(s)
  - `Z_DIN_Terminal_Block.kicad_mod`
- `Z_DIN_Terminals.pretty` — vorbereitet, noch leer
- `Z_DISTRIBUTION.pretty` — vorbereitet, noch leer
- `Z_FI_RCD_4P.pretty` — vorbereitet, noch leer
- `Z_FUSE.pretty` — vorbereitet, noch leer
- `Z_Fuse_Distribution_Bus.pretty` — vorbereitet, noch leer
- `Z_MAIN_SWITCH.pretty` — vorbereitet, noch leer
- `Z_MCB.pretty` — vorbereitet, noch leer
- `Z_MCB_single_pole.pretty` — vorbereitet, noch leer
- `Z_MOTOR_PROTECT.pretty` — vorbereitet, noch leer
- `Z_Motor_Protection.pretty` — vorbereitet, noch leer
- `Z_N_PE_Terminal.pretty` — vorbereitet, noch leer
- `Z_Neutral_Busbar.pretty` — vorbereitet, noch leer
- `Z_PE_Busbar.pretty` — vorbereitet, noch leer
- `Z_RCBO.pretty` — vorbereitet, noch leer
- `Z_RCBO_1P_N.pretty` — vorbereitet, noch leer
- `Z_RCBO_Busbar_1P_N.pretty` — vorbereitet, noch leer
- `Z_RCD.pretty` — vorbereitet, noch leer
- `Z_RCD_Distribution_Bus_4P.pretty` — vorbereitet, noch leer
- `Z_Terminal_Block.pretty` — vorbereitet, noch leer
- `Z_Three_Phase_Busbar.pretty` — vorbereitet, noch leer

## Namensregel

Eine qualifizierte Footprint-ID verwendet das Format `<Bibliothek>:<Footprint>`.
Eine `.pretty`-Bibliothek darf mehrere `.kicad_mod`-Dateien enthalten.
